# Bet API Service

Server will be available here `http://localhost:8080`

### Run with Docker

1. Start container:
```bash
docker-compose up --build
```

2. Stop container:
```bash
docker-compose down
```