# Bet API Service

Server will be available here `http://localhost:8080`

### Run with Docker

1. Start service:
```bash
docker-compose up --build
```

2. Stop service:
```bash
docker-compose down
```